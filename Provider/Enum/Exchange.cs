namespace PMM.Core.Provider.Enum
{
    // Crypto Exchange
    public enum Exchange
    {
        Binance,
        OKX,
    }

    // Will be deprecated
    public enum LibProvider
    {
        Self,
        JKorf
    }
}
